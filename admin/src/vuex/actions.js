import * as types from './mutations'

export const Login = ({commit}) => {
  console.log(types)
  // commit(types.USER_IS_AUTH)
}

export const Logout = ({commit}) => {
  console.log(types)
  // commit(types.USER_IS_NOT_AUTH)
}
