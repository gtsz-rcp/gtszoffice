version: '3.7'

services:

  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
    ports:
      - 5001:5000
    environment:
      - FLASK_APP=./app.py
      - FLASK_ENV=development
    